{
  "name": "drbarryrotz",
  "version": "0.0.1",
  "description": "static site for dr barry rotz",
  "main": "index.js",
  "scripts": {
    "build-styles": "./node_modules/.bin/node-sass-chokidar src/sass/style.scss - build/css/ --recursive",
    "build": "npm run build-styles && npm run copy-assets && npm run build-templates",
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy-assets": "cp -r js build/ && cp -r images build/ && cp -r fonts build/",
    "build-templates": "node bin/build.js"
  },
  "author": "nineohnine",
  "license": "",
  "dependencies": {
    "node-sass-chokidar": "0.0.3"
  },
  "devDependencies": {
    "glob": "^7.1.2",
    "handlebars": "^4.0.11"
  }
}
